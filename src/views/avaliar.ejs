<%- include("includes/head") %>
<style>
    .bd-placeholder-img {
      font-size: 1.125rem;
      text-anchor: middle;
      -webkit-user-select: none;
      -moz-user-select: none;
      user-select: none;
    }

    @media (min-width: 768px) {
      .bd-placeholder-img-lg {
        font-size: 3.5rem;
      }
    }

    .b-example-divider {
      height: 3rem;
      background-color: rgba(0, 0, 0, .1);
      border: solid rgba(0, 0, 0, .15);
      border-width: 1px 0;
      box-shadow: inset 0 .5em 1.5em rgba(0, 0, 0, .1), inset 0 .125em .5em rgba(0, 0, 0, .15);
    }

    .b-example-vr {
      flex-shrink: 0;
      width: 1.5rem;
      height: 100vh;
    }

    .bi {
      vertical-align: -.125em;
      fill: currentColor;
    }

    .nav-scroller {
      position: relative;
      z-index: 2;
      height: 2.75rem;
      overflow-y: hidden;
    }

    .nav-scroller .nav {
      display: flex;
      flex-wrap: nowrap;
      padding-bottom: 1rem;
      margin-top: -1px;
      overflow-x: auto;
      text-align: center;
      white-space: nowrap;
      -webkit-overflow-scrolling: touch;
    }
  </style>
<body class="bg-light">
     
<div class="container ">
  <a href="javascript:history.back()" class="fs-1 "><i class="bi bi-arrow-left"></i></a>
</div>
<div class="container">
  <main>
    <div class="py-5 text-center">
      <img class="d-block mx-auto mb-4" src='<%=  candidata.img %>' alt="" width="72" height="57">
      <h2>Candidata</h2>
    </div>

    <div class="row g-5">
      <% if(errors.length > 0) { %>
        <div class="alert alert-danger" role="alert">
           <%= errors %>
        </div>
    <% } %>
      <%- include("includes/form") %>
      <div class="col-md-7 col-lg-8">
        <h4 class="mb-3">Informaçoes da candidata</h4>
          <div class="row g-3">
            <div class="col-sm-6">
                <h5 class="text-start">Nome:</h5>
                <h6 class="text-start"><%= candidata.nome %></h6>              
            </div>
            <div class="col-12">
              <h5 class="text-start">Idade:</h5>
              <h6 class="text-start"><%= candidata.idade %></h6>  
            </div>
            <div class="col-12">
              <h5 class="text-start">Representa:</h5>
              <h6 class="text-start"><%= candidata.representa %></h6>  
            </div>

          </div>

            

          <hr class="my-4">
      </div>
    </div>
  </main>
</div>

<script>
    function verifAvali(e){
      console.log(e.value)
      const arr = e.value.replace(/[^\d{1}.\d{2}]/g, "");
      e.value =arr;
      if(!(e.value >= 0 && e.value <= 10)){
        e.value = 10;
      }
      console.log(e.value.length)
      if(e.value.length > 4){
        const str = e.value
        e.value = str.substr(0, 4);
      }
      resultado();
      
    }

    function resultado(){
      var n1 = Number(document.getElementById("fantasia").value);
      var n2 = Number(document.getElementById("beleza").value);
      var n3 = Number(document.getElementById("desfile").value);
      var n4 = Number(document.getElementById("desconto").value);
      document.getElementById("resultado").innerHTML = parseFloat(((n1+n2+n3)-n4)).toFixed(2);
      
    }
    function final(){
      var n1 = Number(document.getElementById("fantasia").value);
      var n2 = Number(document.getElementById("beleza").value);
      var n3 = Number(document.getElementById("desfile").value);
      var n4 = Number(document.getElementById("desconto").value);
      
      document.getElementById("f").value = n1;
      document.getElementById("b").value = n2;
      document.getElementById("d").value = n3;
      document.getElementById("des").value = n4;
      document.getElementById("resultado1").innerHTML = parseFloat(((n1+n2+n3)-n4)).toFixed(2);
    }
    function verifAvaliFinal(e){
      console.log(e.value)
      const arr = e.value.replace(/[^\d{1}.\d{2}]/g, "");
      e.value =arr;
      if(!(e.value >= 0 && e.value <= 10)){
        e.value = 10;
      }
      console.log(e.value.length)
      if(e.value.length > 4){
        const str = e.value
        e.value = str.substr(0, 4);
      }
      
      var n1 = Number(document.getElementById("f").value);
      var n2 = Number(document.getElementById("b").value);
      var n3 = Number(document.getElementById("d").value);
      var n4 = Number(document.getElementById("des").value);
      document.getElementById("resultado1").innerHTML = parseFloat(((n1+n2+n3)-n4)).toFixed(2);
      
    }
</script>

<%- include("includes/footer") %>
